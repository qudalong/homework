<view wx:if="{{coverList.length}}" class="card-list clearfix">
  <view bindtap='toCardItem' wx:for="{{coverList}}" wx:key="unique" class='item' data-id="{{item.id}}" >
    <image src="{{item.v_coverimage_path}}" mode='aspectFill' class='cover' />
  </view>
</view>
 